# Environment Configuration
# Copy this file to .env and update with your values

# Default Google Spreadsheet ID (optional - can be overridden in API calls)
GOOGLE_SHEET_ID=your_spreadsheet_id_here

# Default Sheet Name (optional - can be overridden in API calls)
GOOGLE_SHEET_SHEET=Sheet1

# Server Configuration
# Set to 'development' for HTTP mode, 'production' for HTTPS mode
# NODE_ENV=development

# Alternative way to enable dev mode
# DEV_MODE=true

# Port configuration (defaults: 8080 for dev, 443 for production)
# PORT=8080

# Host configuration
# HOST=0.0.0.0

# Optional: SSL Certificate Paths (used in production mode only)
# SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem
# SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
